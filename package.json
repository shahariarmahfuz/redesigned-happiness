{
  "name": "mcq-api-worker",
  "version": "1.0.0",
  "description": "MCQ API using Cloudflare Workers and KV",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "wrangler deploy"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
